# V3R-2 · 真实统一外壳与可用导航验收

日期：2026-07-13
修复分支：`cx/unified-v3-experience-repair`
预览运行时：`v3-repair`（隔离数据命名空间）

## 实际完成内容

1. 新增 `gontu-v3-shell.css` 与 `gontu-v3-shell.js`，把已经确认的黑金、宣纸、古风印章 V3 产品层级接到真实专业页面，而不是继续停留在 Demo。
2. 主导航固定为五个真实区域：言语、数量关系、图形推理、申论批改、AI 教练；每页同时显示当前区域、当前专业路径与“学习中心”返回入口。
3. 言语、数量、平面图推、立体学习中心、截面基础、三视图、动态解题、自由切面、申论与 AI 教练十个正式页面都加载同一套外壳。
4. 自由切面页原本被塞进 248px 侧栏、依赖隐藏横向滚动的空间导航已退出正式交互；空间路径改为页面顶部清晰可见的模块导航。
5. 手机端主导航使用可发现的“学习导航”按钮；空间路径在 390px 下以 3×2 网格完整显示六个入口，不缩小正文、不藏在横向滚动条里。
6. 外壳高度由 `ResizeObserver` 读取真实渲染高度并同步给页面顶部留白，避免不同页面、不同宽度下外壳覆盖训练内容。

## 真实浏览器证据

### 桌面 1440×1000

- `/verbal-reading-pilot.html`、`/quantity-practice.html`、`/mindmap`、`/spatial-learning.html`、`/section-foundation.html`、`/three-view-training.html`、`/geometry.html`、`/csg-section.html`、`/shenlun`、`/ai-coach` 均显示 `gongtu-unified-v3` 外壳。
- 十条路径均满足 `scrollWidth == clientWidth == 1440`；五个主区域入口可见，当前模块状态正确。
- 自由切面页顶部显示完整空间路径，旧侧栏导航为 `display: none`，训练侧栏与 3D 画布从外壳下方开始，不再被遮挡。
- 言语页面标题仍为 46px，确认没有通过缩小正文来换取“一页放下”。
- 申论和 AI 教练在隔离预览账号登录后完成真实页面检查，不以登录重定向代替验收。

### 手机 390×844

- 所有公开专业路径均无横向溢出，主导航按钮可展开并完整显示五个区域；按钮的 `aria-expanded` 会随状态更新。
- 自由切面页实测外壳高度、页面顶部留白均为 170px，首块训练内容从 174px 开始；六个空间入口全部可见。
- 平面图推页将手机次级导航收敛为“图推总览 / 平面规律 / 立体空间”，外壳和页面顶部留白均为 131px，无内容覆盖。
- 验收过程中曾捕获“手机外壳实际 248px、页面只预留 166px”的重叠缺陷；修复真实高度同步后重新测量通过。该失败未被算作通过。

## 自动验证

- 聚焦外壳与既有空间外壳回归：16/16 通过。
- Node 24 + 完整依赖下运行 `npm run test:geometry`：18 个 suite、634/634 通过、0 失败。
- `gontu-v3-shell.js` 语法检查与 `git diff --check` 通过。
- 首次全量命令因新工作树尚未安装依赖且终端默认 Node 18 失败；恢复锁定依赖并显式使用项目 Node 24 后全量通过。此处保留环境事实，不把第一次失败隐藏为成功。

## 边界与下一任务

V3R-2 只证明真实页面已经有统一、可用的产品外壳与导航。顶栏的 AI 教练入口目前仍是通用入口，尚不能证明言语、数量、图推或申论的真实训练上下文已交接。该闭环由 V3R-3 与 V3R-4 完成，不能提前宣称整个 Goal 完成。
