# RC2 最终真实浏览器冒烟验收

日期：2026-07-18  
分支：`agent/admin-vip-console`  
候选标签：`gongtu-rc-20260718.2`  
范围：合并后发布候选，不合并 `main`，不接触真实数据库。

## 隔离运行环境

- 通过 `xixi-dev-system preview start` 启动正式 `start_gontu.py`。
- 临时地址：`http://127.0.0.1:55537`。
- 隔离数据库：`.xds/data/rc2-smoke/data.db`。
- 测试账号只存在于该隔离数据库；验收结束后停止预览，不作为正式账号保留。

## 认证与权限

1. 在统一认证页公开注册普通账号 `rc2_user_0718`，成功进入 `/app`。
2. 普通账号访问 `/admin` 时看到“当前账号不是管理员，不能进入管理台”，没有自动提权，也没有循环跳转。
3. 在服务器本机执行 `tools/bootstrap-admin.py`，成功创建隔离管理员 `rc2_admin_0718`。
4. 管理员从 `/login.html?next=/admin` 登录后进入 `/admin`，页面正确显示管理员身份、2名注册用户和管理操作。

## 关键导航

- 管理后台“进入学习端” → `/app`：通过。
- 学习页“返回首页” → `/`：通过，落点在页面顶部。
- 首页正式首屏 iframe“开始备考” → 顶层 `/app`：通过。
- 首页第一次旧标签页曾继承到底部滚动位置；全新标签页与刷新各复测一次均为 `scrollY=0`，确认是浏览器旧标签状态，不是产品缺陷。

## 桌面与手机视觉门禁

| 页面 | 1280px | 390px | 破图 | 本机 console error/warning |
| --- | --- | --- | --- | --- |
| 首页 `/` | `scrollWidth == clientWidth` | `390 == 390` | 0 | 0 |
| 登录 `/login.html` | 表单可用 | `390 == 390` | 0 | 0 |
| 学习页 `/app` | `1280 == 1280` | `390 == 390` | 0 | 0 |
| 后台 `/admin` | `1280 == 1280` | `390 == 390` | 0 | 0 |

手机端同时人工检查了水墨首页首屏、统一登录卡、学习导航与管理员统计卡，内容未被横向裁切。首页入场动画完成后标题、简介和“开始备考”按钮正常出现。

## 发布边界

- 本记录随最终候选提交进入 `gongtu-rc-20260718.2`。
- 标签仅在候选提交的 GitHub Actions 五项检查全部通过后推送。
- Draft PR #24 保持待审，不自动合并 `main`。
- 本次验收不代表已经部署、开通收费或满足正式生产上线条件。
