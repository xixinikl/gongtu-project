# V3R-6 功能标签与视觉验收记录

状态：**实现与自动/浏览器验证通过，等待用户视觉确认；任务不得提前完成。**

## 本轮新增验收边界

功能标签不是装饰，也不能只改变高亮。每个标签必须按当前页面承担一种真实行为：切换本页主面板、筛选本页数据、切换 AI 模块与 Skill，或进入内容不同的正式页面。不同页面使用自己的功能名称，不放一套万能假标签。

## 桌面真实点击证据

- 平面图推 `/mindmap`：`万象图绎 · 全览`、`图推「3+3」`、`黑白块`、`错题复习` 四个 ARIA tab 分别显示完整知识树、3+3 知识树、黑白块知识树和本人错题复习卡；选中标签的 `aria-selected=true` 随点击同步改变。
- 申论 `/shenlun`：`公文题` 后只剩 2 道公文题，不再混入分析/对策/概括题；`错题追踪` 后主工作区由对话输入切为错题追踪、AI 分析与导出操作，`aria-pressed=true` 同步改变。
- AI 教练 `/ai-coach`：平面图推切换后显示“平面图推老师”、`planar-reasoning-coach v1.1.0`；申论切换后显示“申论批改老师”、`feiyang-shenlun-perspective v1.1.0`，说明老师、简介、历史作用域与后端选定 Skill 均随模块变化。
- 空间训练：`基础截面`、`三视图训练`、`自由切面`、`组合体切割` 分别进入 `/section-foundation.html`、`/three-view-training.html`、`/geometry.html`、`/csg-section.html`；主内容分别是常见几何体截面、11白3黑三视图题、观察与切割实验、截面数据，不是同内容换标题。
- 共享外壳会显示当前页面语义：`平面图推功能`、`空间训练功能`、`申论功能`、`教练模块`；链接可访问名称明确提示“页面内容将更新”或“当前功能”。

## 2026-07-14 用户反馈后的数量与空间修复

- 数量五个标签已连接五条不同路径：`今日训练`、`单题诊断`、`套题策略`、`系统学题型`、`我的复盘`。旧的 `quantity-practice.html?view=single/type/review` 不再冒充相应功能；独立入口分别为 `/quantity-single.html`、`/quantity-topics.html`、`/quantity-review.html`。
- `单题诊断` 每次只从 600 道审核题中抽一题；提交前不返回答案，提交后显示本题答案、原解析、用户关键步骤、卡点与同题型下一题。
- `系统学题型` 将原始细碎标签归一为 17 个可学习题型，按题库的必做/可做/先跳价值、真实题量和建议时间排序；每张卡进入同题型单题训练。
- `我的复盘` 只聚合本人真实单题/套题记录；1 题证据会明确显示“当前 1/5 题，仍需 4 题再判断是否稳定”，并给出下一题直达按钮，不制造稳定弱项。
- 空间页保留原 3D 工具和模型，只将工作区整体下移，让原功能标签完整显示在上方；移除重复绿色旧顶栏，把统一 UI 收为黑金/宣纸层级。AI 动作用 `问西西 · 当前训练` 的次级描边样式，不再挤压最后一个功能标签。
- Chrome 实测 `/csg-section.html`：`scrollWidth=innerWidth=1470`，组合体切割、截面数据和操作指南均仍可见可用。

## 2026-07-14 第二轮用户反馈后的页内结构修复

- 言语二级标签不再把“片段阅读”和“套题训练”写成两个实际相同的功能：现在为 `片段方法` 与 `限时套题`。`片段方法` 在统一学习页内显示中心理解、细节判断、语句理解三类方法；`限时套题` 切换为真实 20 题配置与作答入口。
- 数量五个标签在真实浏览器逐项点击后的映射为：`今日训练 → /quantity-practice.html?view=today`、`单题诊断 → /quantity-single.html`、`套题策略 → /quantity-practice.html?view=set`、`系统学题型 → /quantity-topics.html`、`我的复盘 → /quantity-review.html`。五个标签的标题、卡片、步骤、建议与 CTA 均随标签变化，不再只换高亮。
- 图推 `立体空间` 改为统一学习页内的四个直接入口：基础截面、三视图训练、自由切面、组合体切割；不再先跳到一层静态介绍页。
- 新增内容沿用现有黑金/宣纸组件语法：正文继承原字体，标题使用现有宋体，卡片使用 `#fffdf9`、`#e7dcc7` 与低饱和金色；未新增绿色/蓝色主题或粗重外框。
- 内置浏览器验收入口已切换到当前运行时 `http://127.0.0.1:55194/app?review=v3r6-visible2`；旧端口 `58471`、`58531`、`61117` 不再作为本轮验收地址。

## 2026-07-14 第三轮用户反馈后的可读性与一屏修复

- 词语解释浮层改为完全不透明的 `#fffdf8`，加实线边框和实体阴影；不再透出题目、选项或标签造成文字叠加。
- 言语页从逻辑填空切回 `词语积累` 时直接切换成语卡片主内容；不再要求先点最上层“言语”。桌面片段套题的四个选项改成 2×2，一屏内可同时看见题干、四个选项、上一题/下一题和 20 题进度。
- AI 教练移除重复的产品级顶栏，只保留老师模块、对话、问题本和“返回原训练”；历史失败不会在刷新后常驻，只有当前可重试 run 才显示重试按钮。页面明确显示真实 provider/model/completed 状态，训练线程还显示“已核验本次训练 · N 条题目/作答证据”和后端选中的 Skill。
- Provider 第一次返回不符合 Skill Schema 时只自动重试一次并加强结构约束，第二次仍非法才 fail closed。本轮真实调用 `deepseek-chat` 成功返回 `completed`，随后内置浏览器从正式 AI 页面发送问题也得到真实非空回答。
- 数量单题首屏先选择按价值排序的专项，进入题目后题干、2×2 选项、关键步骤、卡点、提交按钮和右侧四步顺序均在桌面一屏内；没有右侧溢出。
- 空间页保留原有六个功能标签；`自由切面` 和 `组合体切割` 的 3D 工作区在标签下方完整展开，去掉重复旧顶栏，桌面首屏可见模型、操作和两侧控制区。

## 2026-07-15 第四轮用户反馈后的内联学习与空间导航收口

- 言语与数量仍使用 `/app` 原有两层导航，二级标签直接在下方宣纸内容区换真实内容；不新增跳转外壳。逻辑填空恢复关联词语/成语入口，词语释义、例句、收藏与加入复习继续调用原词库能力。
- 题目右侧改为真实“问西西”对话栏：服务端每次消息重新按当前用户和题目引用核验上下文，历史消息可滚动且刷新后保留。本轮真实询问“先讲方法，不要直接告诉我答案”得到当前逻辑填空题的方法提示，未泄露答案。
- 言语复盘、词语积累、逻辑填空、数量今日训练、单题诊断和数量复盘已分别在 `inline-v5` 浏览器会话检查；正文、选项、底部动作和右栏在桌面首屏内形成完整层级。
- 空间页不再显示重复黑色产品栏；阶段导航使用低饱和金色与宣纸纹理，固定居中，`返回主要学习环境` 固定在右侧。`csg-section.html` 的 3D 工作区整体下移，阶段标签不再挤入左侧栏。
- “完成本阶段学习”可见按钮已删除；原本只用于空间 visit 复盘记录，现改为进入阶段后自动记录，不再占用界面。几何页“进入题库”按钮也已删除。
- 浏览器截图：`output/playwright/inline-v5-logic.png`、`inline-v5-vocab.png`、`inline-v5-quantity.png`、`inline-v5-quantity-single.png`、`inline-v5-quantity-review.png`、`nav-v5-csg.png`、`nav-v5-foundation.png`、`nav-v5-geometry.png`。当前地址 `/app?review=inline-v5`、`/csg-section.html?review=nav-v5`、`/geometry.html?review=nav-v5` 均返回 200。

## 2026-07-15 交付前完整审计

- 真实桌面浏览器重新逐项点击言语与数量二级标签。逻辑填空同时显示真实题目、词语/成语联动和可回看历史的右侧 AI；数量的 `今日训练`、`单题诊断`、`套题策略`、`系统学题型`、`我的复盘` 分别显示题目、套题、17 类按价值排序题型或本人真实复盘，不是只换高亮。
- 独立 AI 教练用 `logic-question:haihai_logic_001` 重新进入后显示 `logic-fill-coach v1.1.0`，页面明确说明服务端按当前账号核验训练引用；返回链接精确恢复 `/app?review=completion-audit-ai`，没有把答案或任意 Skill 放入网址。
- `/mindmap`、`/shenlun`、`/csg-section.html`、`/section-foundation.html`、`/geometry.html` 在 1440px 均无横向溢出；空间页只有居中的四阶段标签和右侧“返回主要学习环境”，未恢复重复黑色产品栏。Three.js 仅报告 `PCFSoftShadowMap` 已弃用并自动使用 `PCFShadowMap`，没有运行错误。
- 在 390×844 真实浏览器重新检查主学习页、组合体切割、基础截面、自由切面、申论和 AI 教练，全部 `scrollWidth === clientWidth` 且保留可见返回入口。手机二级标签补上 `flex: 0 0 auto`，不再把“词语积累 / 逻辑填空”等压缩为图标和单字；标签可横向滚动查看完整名称。
- 本轮聚焦回归为 Node 40/40、Python 7 个独立模块 54/54；浏览器控制台无业务报错。`GET /api/health` 返回 200。上述证据只证明已到可交付检查状态，用户视觉确认仍是最后门禁。

## 2026-07-15 空间顶栏严格同构与数量渲染收口

- 四个空间页面的顶部不是分别模仿，而是共同调用一个 `makeBrand()`：左侧模块名固定使用同一外框、同一“公”图章、同一标题/副标题排版；中间四阶段标签和右侧“返回主要学习环境”也由同一工具栏组件生成。只有模块标题文本按页面变化。
- 真实浏览器读取四页计算样式：左侧模块块均为 `250×46px`，图章均为 `46×46px`，标题均为 `17px / 21.25px`、副标题均为 `11px / 13.2px`，左上坐标均为 `x=24px、y=12.5px`；工具栏高度均为 `72px`。这证明左侧模块名的实际几何和字体也完全一致。
- 数量页偶发蹦回旧“识别题型”板块的根因是旧异步题库渲染与新内联标签共同写入 `#learningZone`。现已收口为单一渲染所有者：当前不在词语页时，旧渲染不能再覆盖数量、逻辑填空或复盘内容；没有更换现有主题与版式。
- 内联刷题页补齐可见的上一题/下一题和键盘左右键。输入框、文本域、下拉框及可编辑区域获得焦点时不会截获按键；数量单题还会保留本次会话题目历史，左右键回看时不会重新造题。
- 聚焦回归 `29/29` 通过；`/app`、四个空间页面和后端根/OpenAPI 均返回 200。当前检查入口为 `http://127.0.0.1:55194/app?review=shared-header-and-keys-20260715`，用户视觉确认仍是最后门禁。

## 真实 AI 证据

- 在隔离预览 `/ai-coach?module=verbal.reading` 发送“片段阅读主旨题，如何快速区分主旨与意图？请只给三步。”。
- 页面返回 `deepseek · deepseek-chat · 1.2.0 · completed`，并给出具体三步方法；不再把空字符串渲染成“证据不足”的伪成功。
- `coach-chat` Schema 要求非空 `answer`；后端同时校验 `minLength`，非法空回答 fail closed。

## 六个学科模块的当前闭环审计

| 正式模块 | AI module | 运行 Skill | 可信上下文与返回证据 |
| --- | --- | --- | --- |
| 片段阅读 | `verbal.reading` | `verbal-reading-skill` v1.2.0 | 服务端按 JWT 读取本人 session/attempt；未交卷答案隐藏；返回同一 `?session=`。 |
| 逻辑填空 | `verbal.logic_fill` | `logic-fill-coach` v1.1.0 | 只加载当前语境、选项与逻辑填空窄适配；不加载整份 huasheng13 词表。 |
| 数量关系 | `quantity.practice` / 正式套题别名 `quantity.exam` | `quantity-coach` v1.1.0 | 单题和套题都只传服务端引用；返回单题 session 或原套题 session。 |
| 平面图推 | `reasoning.planar` | `planar-reasoning-coach` v1.1.0 | 本人错题活动由服务端解析；返回原题详情。 |
| 立体图推 | `reasoning.spatial` | `spatial-reasoning-coach` v1.1.0 | 本人空间 visit/completion 记录由服务端解析；返回基础截面、三视图、自由切面或组合体原页。 |
| 申论批改 | `shenlun.review` | `feiyang-shenlun-perspective` v1.1.0 | 只有成功批改活动进入 AI 上下文；失败不造学习事实；返回原 `?question=`。 |

上述六模块均由 allowlist Registry 选 Skill，页面不能通过 query 或用户文本指定任意 Skill。`return_url` 只接受同源路径，外部地址归一化为 `/app`；跨用户 activity/session/thread/run 读取返回 404。自动证据由 Python AI/Skill/业务模块回归和 `tests/training-ai-handoff.test.mjs` 共同覆盖；用户视觉确认仍是 V3R-6 的独立最后门禁。

## 390px 手机宽度证据

- `/mindmap`：4 个功能 tab 保留且可点击，点击“黑白块”后显示对应知识树；`scrollWidth=innerWidth=390`。
- `/shenlun`：全部、公文题、分析题、对策题、概括题、飞扬对话、错题追踪均保留；`scrollWidth=innerWidth=390`。
- `/ai-coach`：8 个模块按钮均保留；`scrollWidth=innerWidth=390`。

## 自动回归

### 2026-07-15 申论问题追踪与统一导出收尾

- 用户界面将“错题追踪”收口为“问题追踪”：正式批改继续自动加入；普通问答成功后返回本人历史记录标识，并由用户在老师回复下方显式点击“加入问题追踪”，普通聊天不会被自动判定为错题或薄弱证据。
- `POST /api/shenlun/mistakes` 只按 JWT 当前用户读取历史记录；A 用户不能加入 B 用户的问答。追踪快照保存题目、材料、作答要求、本人提问/作答和老师回复，题源后续变化不会让已保存卡片丢失正文。
- Word 与 PDF 不再各自拼接一套不一致的 HTML，统一由 `buildTrackingDocument()` 生成同一份经过转义的完整问题追踪文档；两种导出都包含题目、材料、要求、作答（适用时）和老师回复。
- 专项证据：`tests/test_shenlun_api.py` 15/15、`tests/shenlun-phase4-ui.test.mjs` 9/9、`py_compile` 与 `git diff --check` 通过。Node 24 全量 658/658；AI Coach、Skill Registry、平面、数量、申论、空间、片段阅读七个后端文件分进程 55/55。
- 申论初次独立数据库预览 `http://127.0.0.1:51314/shenlun?review=v3r6-final`：1280px 与 390px 均 `scrollWidth === clientWidth`；问题追踪卡显示完整题目、材料、要求和回复，Word/PDF 操作可见，浏览器 console error/warn 为 0。该次隔离工作树未继承原项目 `.env`，因此真实问答诚实显示服务不可用；随后已在不复制、不输出 Key 的前提下修复预览环境继承，见下一节。
- 本节完成 V3R-6 的实现与代理验收，不替代用户本人对最终视觉的确认；主 Goal 继续保持 5/6，直到用户实际看页确认。

### 2026-07-15 数量四模式与 DeepSeek 配置恢复

- “识别与填空”大标签旁恢复四个小标签：`全部题型 / 易忘攻克 / 步骤填空 / 题型识别`；小标签只切换当前数量内容，不跳页。右侧重复的“原数量训练”说明框删除，主内容扩展为单栏。
- 根因修复：账号后端保存的旧数量卡组可能缺少后来加入的 `blank_steps`，拉取后会覆盖本地内置卡组。现在 `hydrateBuiltinMathDeck()` 在本地初始化和账号同步后都做幂等补全，保留既有学习状态，同时恢复六张步骤填空卡；初始化阶段不会反向覆盖后端账号数据。
- AI Key 并未丢失：原项目 `backend/.env` 中 `DEEPSEEK_API_KEY` 仍配置。最新隔离预览只在启动进程环境中继承该文件，不复制或输出密钥；`/api/ai-coach/modules` 安全摘要返回 `configured: true`、`deepseek-chat`。
- 真实浏览器证据：`http://127.0.0.1:54041/app?review=quantity-four-modes-key` 在 1280px 显示四个相邻小标签、牛吃草步骤填空卡，`scrollWidth === clientWidth`，旧右框不可见；切到“题型识别”后题目真实变化且 URL 不变。用当前数量题发送“只提示第一步”的问题，DeepSeek 返回方法提示；独立 `/ai-coach` 显示“AI 教练可用”。

### 2026-07-15 竖排模式、思考反馈与申论后端统一

- 数量最终排版：题目卡右侧预留 `108px`，四个模式在卡片旁纵向排列；真实 1280px 页面测得题目卡约 `1046px`、标签栏 `108px`、`tabsLeft > cardRight`、无横向溢出。窄屏按媒体查询回落为题目下方横排。
- 申论失败根因：统一 AI 配置接受 `DEEPSEEK_API_KEY`，旧 `backend/src/grader.py` 与 `mistake_tracker.py` 只读取 `LLM_API_KEY`，因此同一进程中 AI 教练可用但申论在调用前直接 503。现统一为优先 `LLM_API_KEY`、兼容 `DEEPSEEK_API_KEY`，不复制、不输出真实 Key。
- 申论卡顿根因：页面依赖外部 unpkg Vue，真实日志中 HTML 后约 11 秒才开始题库请求；现在 Vue 固定为 npm 本地依赖，静态资源返回 200/约 3ms，浏览器刷新后 10 张题卡约 249ms 出现。
- 后端响应性：申论普通问答、正式批改、薄弱点分析原先在 `async` 路由中同步等待模型，现统一通过 `asyncio.to_thread` 执行，模型生成期间不再堵塞其他本地接口。
- 真实 AI 证据：申论普通问答连续两次返回，等待时“老师思考中…”可见；选择 `q3-1` 提交正式作答后约 19 秒返回总体评价、五个评分维度和三条改进建议，无 `provider_unavailable`。通用 AI 教练等待时显示“思考中”，完成后把 1/2/3 三步拆行，用户原问题不被错误拆分。
- 自动证据：Node 全量 `663/663`，申论 Python `15/15`，前端专项 `28/28`，`git diff --check` 通过。
- 当前预览：`http://127.0.0.1:57570/app?review=quantity-vertical-thinking`；申论：`http://127.0.0.1:57570/shenlun?review=formal-grade-restored`。

### 2026-07-15 AI 小数排版与数量卡片收尾

- AI 排版根因：旧规则 `^(\d+)[.、]\s*` 会把行首 `5.6` 识别成“第 5 步 + 6”，视觉结果变成 `15 - 56 - 6 = 34`。现仅把 `1. `（点号后必须有空格）或 `1、` 识别为列表，小数与比例不再进入步骤组件。
- 真实旧消息复验：`5.6 / 2.4 / 3.4 / 1.6` 全部保留；错误步骤数 `0`；长答案分成 `4` 个句群段落，识别 `10` 个分数/算式标记；同一账号的逻辑填空回答仍显示 `3` 个真正步骤，用户问题中的数字不被拆分。
- 数量留白修复：仅对 `recite / difficult` 两个模式设置视口型最小高度并让卡片填满剩余空间；720px 验收视口下两者容器均高 `562px`、卡片 `520px`、距底部 `11px`、横向溢出 `0`。步骤填空、题型识别和其他页面不受影响。
- 回归：Node 全量 `663/663`，`git diff --check` 通过。

### 2026-07-15 数量导航竞态收尾

- 复现：首次切到数量会异步加载“今日训练”；在请求完成前点击“系统学题型”或“识别与填空”，旧响应可能晚到并覆盖新页面，造成正文是系统题型、顶部仍高亮识别填空，随后识别填空看似打不开。
- 修复：每次主标签激活递增 `navigationVersion`；数量单题、套题、题型和复盘的异步渲染只在请求版本仍为最新时写入 DOM，过期成功或失败响应均静默放弃。
- 浏览器证据：30ms 快速切换后等待 1.5 秒，“系统学题型”保持 `surface=quantityType`、主标签高亮正确、标题正确、小标签隐藏并回到主导航；再点“识别与填空”等待 1.5 秒，保持 `surface=quantityLegacy`、默认 `steps`、四小标签竖排、真实卡片存在、无空卡片提示、无横向溢出。
- 回归：新增导航竞态合同测试，Node 全量 `664/664`，`git diff --check` 通过。
- 自动证据：`tests/v3-functional-labels.test.mjs` 7/7，Node 24 全量 660/660，`git diff --check` 通过。

- 新增 `tests/v3-functional-labels.test.mjs`，覆盖页面专属名称、空间正式目标、平面真实 panel、申论筛选/工作面板和 AI 模块状态。
- 功能标签、统一外壳、数量三页与 AI 上下文聚焦回归：24/24。
- `npm run test:geometry`：657/657，通过。
- Python 测试模块按 `backend/requirements.txt` 逐文件独立进程：80/80，通过；AI 教练新增“首次 Schema 非法、第二次合规”重试回归，专项 15/15。
- `git diff --check`：通过。

## 剩余验收

预览当前由独立 `55194` 运行时提供，`GET /app` 返回 200；后端根路径与 OpenAPI 均返回 200。完整前端回归为 657/657，后端六模块、Skill Registry 与安全合同逐模块回归为 54/54。真实浏览器再次逐页读取四个空间页面的共用顶部：左侧模块容器、图章、标题/副标题字号与间距、中间阶段标签和右侧返回的尺寸与坐标一致，仅模块名称文本按页面变化。最新交付检查地址为 `http://127.0.0.1:55194/app?review=goal-audit-final-20260715`。必须继续等待用户视觉确认；若用户认为字号、布局、层级、文案或功能仍与确认 Demo 不符，继续修正。只有用户明确确认后，才可验证 V3R-6 并完成整个 Goal。
