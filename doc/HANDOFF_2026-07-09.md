# 2026-07-09 项目交接文档

本文件是本轮上传 GitHub 前的最新交接入口。聊天摘要只能作辅助，后续 Agent 必须先读仓库文件。

## 基本事实

- 项目：公途空间几何 / 考公立体图推训练
- 仓库：`https://github.com/xixinikl/gongtu-project.git`
- 当前分支：`cx/lesson-015-dynamic-explore`
- 当前基线提交：`4350dcb chore: add static dev server script`
- 本轮交接任务：`HANDOFF-20260709 docs: 写入当前交接并上传 GitHub`
- 本轮提交范围：当前分支上动态解题、三视图训练、基础环境与项目记忆相关改动
- 明确不提交：`.playwright-cli/` 本地自动化日志、根目录 `真正的成语.xlsx`

## 当前项目状态

当前主线正在做 M5A：考公立体图推动态解题与讲解。最近已完成的核心切片如下：

1. 动态解题页 `reasoning-lesson.html`
   - 已建立候选答案、真实 3D 截面、二维实时截面、逐项验证的联动页。
   - 当前版本按“先选答案，再解析”处理；未选 A/B/C/D 前不展示真实答案对比。
   - 真实截面 SVG 已改为按当前 Three.js 相机方向投影，避免 3D 和下方截面图方向不一致。
   - C 类选项只写“类型可验证”，不能再写成“完全对上”。

2. 三视图训练页 `three-view-training.html`
   - 已录入 50 道黑白方块三视图题，每 5 题一组。
   - 答题前隐藏真实 3D 模型，答题后才展示模型验证。
   - 视图方向统一为：以主视图为基准，左转是左视图，右转是右视图，上看是俯视图。
   - 页面已从三栏临时 demo 改成更正式的两栏训练工作台。

3. 本地开发环境
   - `npm run dev` 已改成自检 8089：端口未开时后台启动，已开时复用。
   - 新增 `npm run dev:status`、`npm run dev:stop`。
   - 新增 `npm run doctor`，只检查环境，不安装、不删除、不启动长期服务。

4. 项目记忆
   - 根目录 `AGENTS.md`、`doc/USER_PREFERENCES.md`、`doc/ENVIRONMENT.md`、`doc/CODEX_PORTABILITY.md` 已加入仓库，便于换电脑或全新 Codex 接手。
   - 用户反复纠正过的问题已写入 `错误复盘.md`。

## 当前验证结果

最近一次完整功能验收记录在 `CURRENT_STATUS.md`。本轮上传前额外运行：

- `npm run doctor`：未全绿。Node.js 为 18.20.8，项目要求 20+；Python 为 3.9.6，项目要求 3.10+。8089 和 8888 均已监听。
- `git diff --check`：通过。
- `node --check reasoning-lesson.js && node --check three-view-training.js && node --check tools/doctor.mjs && node --check tools/dev-server.mjs`：通过。
- `node --test tests/reasoning-lesson-layout.test.mjs`：17/17 通过。
- `node --test tests/reasoning-case-fixtures.test.mjs`：12/12 通过。
- `node --test tests/three-view-training.test.mjs`：6/6 通过。
- `npm run test:geometry`：572/572 通过。

上一次功能侧浏览器验收记录见 `CURRENT_STATUS.md`，包括动态解题页和三视图训练页的真实浏览器检查。

后续 Agent 不得把本轮 `doctor` 失败说成通过；若要继续开发，应先切到 Node 20+ 和 Python 3.10+。

## 当前未完成 / 待验收

1. 等用户最终验收 `LESSON-015F`：动态解题页是否真正满足“3D 当前方向”和“下方截面图方向一致”的体验。
2. 动态解题页仍有产品体验待打磨：用户此前指出截面正面、逐项验证话术、题图裁剪和选项选择流程都需要持续核对。
3. 三视图训练下一步是 `LESSON-016`：建立四类训练总入口，整合动态解题、三视图训练、基础截面和几何实验室。
4. `cone-box-001` 的用户候选答案与既有正式答案存在冲突记录，不能默默覆盖，需要回看原视频确认。

## 新 Agent 接手步骤

按顺序执行：

```bash
git status --short --branch
git remote -v
npm run doctor
```

然后读取：

1. `AGENTS.md`
2. `CURRENT_STATUS.md`
3. `TASKS.md`
4. `错误复盘.md`
5. `doc/USER_PREFERENCES.md`
6. `doc/ENVIRONMENT.md`
7. `doc/CODEX_PORTABILITY.md`
8. 本文件

常用入口：

```bash
npm run dev
open http://127.0.0.1:8089/reasoning-lesson.html?case=pyramid-cylinder-001&verify=front-facing
open http://127.0.0.1:8089/three-view-training.html
```

## 必须避免再犯

- 不能把“几何类型可行”写成“选项完全对上”。
- 不能在用户未作答前暴露真实模型、真实截面或逐项解析。
- 不能只看 DOM 或指标就说体验通过，必须结合真实浏览器截图和交互矩阵。
- 不能把本地服务没启动说成浏览器拦截，先查端口和 HTTP 200。
- 不能提交本地运行产物、数据库、日志、真实密钥或用户临时文件。
