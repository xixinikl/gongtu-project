# 当前开发状态

更新时间：2026-07-18

当前分支：`agent/admin-vip-console`

## 2026-07-18 生产安全底座已合入唯一候选分支

- 基线固定为发布候选标签 `gongtu-rc-20260718.1`，在独立分支与独立工作树开发，不改用户真实数据库，不直接合并 `main`。
- SEC-1A 已完成：网页注册固定创建普通用户；`tools/bootstrap-admin.py` 仅在服务器本机通过双次隐藏密码输入创建或提升初始管理员，已有管理员时拒绝执行。管理员/VIP与JWT专项 7/7、Ruff、mypy 26文件、Bandit中高危0、真实命令行空库初始化与二次拒绝均通过。
- SEC-1B 已完成：登录页明确公开注册只创建普通账号，并给出服务器本机初始化命令；统一认证专项新增旧危险文案禁止回归断言，8/8通过。
- SEC-2 已完成：登录/注册按账号指纹和来源地址执行双层滑动窗口限流，429携带`Retry-After`；成功登录只清账号桶，不能清来源总限额；代理头默认忽略，仅直连地址位于`GONTU_TRUSTED_PROXIES`时按可信链解析；用户名/密码限制为3—64与8—256字符，不存在账号仍走等成本密码校验。
- 限流专项3/3、管理员/VIP 4/4、JWT 3/3、Ruff、mypy 27文件、Bandit中高危0通过。该限流器是有界的单进程内存实现；SEC-5必须在多worker部署时停止上线或要求共享限流存储。
- SEC-3 已完成：数据库v12新增追加式管理员审计表；授权、VIP/积分、AI访问策略、词库/牌组/申论/整用户删除均在业务同一事务内写流水。审计失败注入证明业务回滚；管理员可查最近1—200条，未认证请求401；用户删除后审计仍保留，详情只来自受控字段。
- 管理员专项5/5、旧库迁移/恢复2/2、Ruff、mypy 27文件通过。
- SEC-4 已完成：`tools/gontu_db.py` 提供在线备份、完整性验证和只写新目标的恢复命令；副本0600、按工具命名模式保留，损坏副本和既有恢复目标均拒绝。专项3/3、Ruff与语法检查通过；操作说明写明异盘/加密远端副本、24小时新鲜度和恢复演练停止条件。
- SEC-5A 已完成：正式入口在生产模式必须提供显式HTTP(S)来源白名单，缺失、通配符、带路径/查询/片段的伪origin均拒绝启动；开发模式仍可本地使用。专项3/3、Ruff、mypy 27文件通过，环境示例已补齐。
- SEC-5B 已完成：`tools/production_readiness.py` 只有在生产模式、非示例JWT/AI密钥、HTTPS来源、合法代理、单worker、正式库0600且完整并有管理员/审计表、24小时内0600完整备份、独立恢复库全部通过时才返回0。专项5/5、Ruff与语法检查通过。
- SEC-1A—SEC-7 已完成：独立审查无代码阻断项，并修正三处与当前安全实现冲突的旧版自动提权记录。20个Python测试文件逐进程通过；Node 673/673；依赖树、Ruff、mypy 27文件、Bandit中高危0、doctor 0 fail。最终安全分支 GitHub Actions run `29647861867` 五项全部成功。
- PR #25 已在 2026-07-18 合并到唯一候选分支 `agent/admin-vip-console`，merge commit 为 `a1a836e`；该合并提交对应 GitHub Actions run `29647898525` 五项全部成功。PR #24 仍保持 Draft、OPEN、MERGEABLE，`main` 未合并也未改动。
- 当前无进行中任务。事实边界：未部署、未开通收费；当前电脑没有真实生产数据库、异盘备份和独立恢复副本，因此生产门禁应诚实返回不可上线。
- 后续建议：为合并后的候选创建新的 RC 标签并做最终浏览器冒烟验收；确认后再决定是否让 PR #24 进入 `main`，本轮不执行。

## 2026-07-18 版本收口完成（REL-1A—1C）

- 用户已确认找回的首页、功能页与当前交互版本，当前不再增加新功能，先建立唯一发布候选和可复现门禁。
- 唯一候选：`agent/admin-vip-console`；REL-1A `4564a54` 与 REL-1B `9987196` 已推送；最终候选事实以 `gongtu-rc-20260718.1` 标签为准；Draft PR #24 当前 `MERGEABLE`。
- 已完成本地收口基线：项目专用 Python 3.11.15 环境已建立；`npm run doctor` 为 0 fail；Node 全量 672/672；Python 16 个测试文件成功，其中 unittest 共 85 项，另有 1 个脚本型跨平台检查。
- REL-1A 已完成：PR 工作流不再只监听 `main` 基线；新增独立后端测试作业；临时旧库迁移/恢复 2/2 通过；Ruff 发现并修复数量模块漏导入 `Any`，未改运行逻辑。
- REL-1B 已完成：排除本地 `backend/venv` 后 mypy 26 文件无问题，Ruff 通过，Bandit 中高危 0；AI 教练 15/15、申论 15/15、Python 全量 87 项通过。只补证据列表变量与申论历史 id 类型，不改接口行为。
- REL-1C 已完成：发布候选说明写清唯一分支、正式入口、包含/排除项、可复现命令、SQLite 备份/整库恢复与停止条件；不具备本轮归因能力的 PR 临时合并提交空格检查已移除，本轮 diff 仍通过本地 `git diff --check`。
- 远端证据：GitHub Actions run `29640065363` 的 frontend-check、backend-tests、lint、type-check、security 五项全部通过。最终审计提交推送并再次触发检查后，`gongtu-rc-20260718.1` 标签指向该提交。
- 收口结论：当前版本已冻结为封闭内测候选，不是公网收费生产版；PR #24 保持 Draft，不合并 `main`，下一阶段进入生产安全底座。
- 本任务边界：只修改 CI、迁移/恢复测试、发布候选清单和两份审计文件；不直接把相对 `main` 的 196 个历史提交批量合并，不改用户真实数据库。

## 2026-07-18 管理员 / VIP 纵向闭环（历史基线，安全初始化规则已被上方 SEC-1A 取代）

- 本分支从已推送的首页与功能页接线提交 `fd8811b` 新建，未覆盖已验收的首页、功能页和问西西实现。
- 管理员端改用统一 `gontu_token`：未登录进入 `/admin` 会回到 `/login.html?next=/admin`，普通用户不会在登录页与后台之间循环跳转；后台不再维护第二套账号登录。
- 当时的实现曾让第一个注册账号自动成为初始管理员；该行为已由上方 SEC-1A 关闭。当前公开注册始终是普通用户，初始管理员只能在服务器本机通过 `tools/bootstrap-admin.py` 显式初始化。
- 管理员可查看跨模块学习概览、授予或取消其他账号管理员权限、设置 VIP 状态/到期日/AI 积分，并且不能取消自己的管理员权限。
- 数据库新增 `is_vip`、`vip_expires_at`、`ai_credits` 与 `app_settings.ai_access_mode` 的幂等迁移；默认仍是 `free`，不会影响当前 AI 学习功能。
- “VIP / 积分模式”目前是可持久化的运营策略预设，不代表已完成 AI 扣点与消费流水。正式收费前仍需把统一额度校验、原子扣点、退款/补偿与审计流水接入各 AI 接口。
- 自动验证：管理员/VIP 专项 4/4；全部 Python 测试文件逐进程通过；Node 全量 670/670；脚本解析、`py_compile` 与 `git diff --check` 通过。
- 历史浏览器验证：隔离数据库和 `http://127.0.0.1:53996` 中曾完成旧版首个账号自动提权、普通账号创建、VIP=88/到期日保存、管理员授权、策略切换与恢复免费；视觉证据仍有效，但旧版自动提权路径已经移除，不得作为当前初始化步骤。

## 2026-07-18 跨电脑救援快照（当前优先事实）

- 用户已在真实后端入口确认：本分支中的首页原型与功能页是另一台 Mac 上最后认可的版本。
- 已确认首页：`doc/prototypes/homepage-middle-ink-morph.html`。
- 已确认功能页：`智学成语-高级版.html`，正式后端入口为 `/app`。
- 已确认申论入口：`shenlun.html`，正式后端入口为 `/shenlun`。
- 注册、登录、用户身份接口与真实 DeepSeek AI 请求均已在隔离数据库中验证成功。
- 本分支是从 `cx/phase6-e2e-hardening` 的 `dd2c032e6dd9f8e02a585847aa3ea4e544acb75b` 建立的原样救援分支；不得把其他工作树的管理员/VIP冲突改动直接整批覆盖进来。
- 完整来源、文件校验值、排除项、验证证据和后续接手步骤见 `doc/recovery/RECOVERED_FINAL_20260718.md`。后续 Agent 必须先读该文档再继续首页接线或管理员/VIP开发。

当前 Goal：`unified-v3-experience-repair`

当前视觉子 Goal：`homepage-content-led-guofeng-redesign`（用户已确认找回的水墨首页与功能页；正式首页接线已完成，等待用户视觉确认）

当前阶段：V3R-6 正式首页接线已通过代理验收；水墨首页为 `/`，功能页保持 `/app`

当前执行结果：后端 `/` 返回已确认的水墨首页，两个正式片段读取 `/index.html` 防止递归；iframe 内真实“开始备考”按钮进入顶层 `/app`，功能页“返回首页”可回到 `/`。隔离后端已重启于 `http://127.0.0.1:54670`，源数据库未改；注册、登录、用户身份、数量、申论、AI 教练和真实 DeepSeek 均成功。用户标注的五模块水墨空白源于根路由下 `./assets/` 被解析为不存在的 `/assets/`，三处首页自有资源已改为 `/doc/prototypes/assets/`；真实浏览器确认言语水墨图恢复、切到数量后底图位置 25%、Spark 状态从 morphing 回到 settled 且渲染停止。标签串色已修复：图推→数量后仅数量主标签高亮，图推子导航会收起。内嵌“问西西”已增加右栏小面板与独立悬浮窗双模式：悬浮窗可拖动、锁定、右下角自由缩放、一键放大/缩小、还原右栏，并在换题后保留模式、位置和尺寸；失败 run 显示“重新请求”，不丢题目与提问。隔离预览 `http://127.0.0.1:58909/app` 的新测试账号已在数量单题和言语逻辑填空各完成一次真实 DeepSeek 对话，均正常返回；旧账号那次“AI暂时不可用”已确认是单次 `provider_invalid_schema`，不是整项 AI 服务停机。Python AI/Skill 30/30、Node/几何全量 670/670（加入尺寸断言后待最终重跑）、首页专项 4/4均通过。图推总览的“平面图推进入规律导图……”来自找回快照 `doc/prototypes/verbal-ai-native-demo-v3.js`，本轮未改。用户在 1167px 视口标注的 `/ai-coach` 挤压已定位：基础页原本在 1120px 才降为两栏，但 `doc/prototypes/ai-coach-gongtu-theme.css` 把断点提前到 1180px，并将 `.ledger` 移到整行；固定 `100vh` 又让主对话与问题本共享两行高度，因此聊天区和空问题本同时被压扁。该项仅诊断，尚未改动，等待用户决定是否把独立 AI 页纳入本轮视觉收口。当前等待用户继续视觉确认，确认后再提交和推送本次独立分支。

最终回归补记：加入悬浮框尺寸按钮断言后的 Node/几何全量已重跑并保持 670/670；上段“待最终重跑”已完成。用户复核指出“大框不等于有排版”后，大窗新增独立阅读排版：长回答按语义标点分段，回答卡加宽并使用 16px 字号和更大行距，输入区固定到底部且发送按钮持续可见；真实浏览器已确认同一回答拆为 2 个段落。

夜间视觉验收补记：用户提问并未丢失，隔离数据库中 `user/assistant` 角色与内容均完整；问题来自悬浮窗移出学习区后未继承金色 CSS 变量，导致用户气泡成为白字透明底，再叠加历史区滚动而看似消失。悬浮窗现自带完整公途配色变量，最新提问使用金色右对齐“你问”气泡并固定在回答上方；左下角新增第二个“缩小”入口。真实浏览器完成刷新、历史恢复、左下角缩小、右上角放大与发送区视觉验收：2 条用户提问均存在，最新显示“你问：不懂”，两个缩小入口均可见且可操作；Node 专项 10/10、全量 670/670。

## 当前 Goal 进度（本节优先于下方历史 Phase 记录）

- [x] V3R-1 冻结 V3 Demo、十条真实页面路径和桌面/手机失败基线。
- [x] V3R-2 将统一黑金/宣纸 V3 外壳接入十个正式页面，修复空间导航隐藏横向滚动与手机内容覆盖；浏览器桌面/手机实测通过，Node 634/634。
- [x] V3R-3 言语和数量真实训练引用已接入 AI；未交卷答案保持隐藏，返回恢复同一 session；Node 639/639、AI 后端 13/13。
- [x] V3R-4 平面/立体/申论正式入口与往返实测通过；Skill 为“模块主 Skill + huasheng13 按题型窄适配”，申论为飞扬核心加题型补充。
- [x] V3R-5 Python 15 文件独立进程 77/77、Node 全量 643/643，记录、Skill、用户隔离与安全合同证据已归档。
- [ ] V3R-6 当前正式 Goal 进度 5/6（83%），实现与代理验收约 99%。数量“识别与填空”四个小标签已从主导航移到收窄题目卡右侧竖排，移动端回落为横排；“全部题型 / 易忘攻克”学习区按视口撑满，720px 验收视口下距底部约 11px，不再留下大块空白。数量导航新增版本门禁，首次取题的旧异步响应不能再覆盖后来点击的“系统学题型 / 识别与填空”；旧渲染器只在明确切换四个小标签时放行，所有晚到回调通过受保护入口。小标签点击判断已收窄到 `.quantity-legacy-subtab`，不会再因 body 上的模式状态把“今日训练 / 单题诊断 / 套题策略 / 系统学题型 / 我的复盘”误判成“步骤填空”；五条退出路径在旧 `?demo=verbal-ai-v3` 直达入口逐条通过。登录后旧后端卡组会幂等补回内置 `blank_steps`，步骤卡不再消失，右侧“原数量训练”说明框已删除。申论已统一兼容 `DEEPSEEK_API_KEY` / `LLM_API_KEY`，普通问答、正式五维批改均真实返回；三条申论模型调用均通过 `asyncio.to_thread` 避免堵塞整个后端。申论 Vue 改为本地依赖，真实首屏 10 题约 249ms 出现；通用 AI 与内嵌问西西均增加“思考中”气泡。AI 回复排版已严格区分列表编号与小数，真实数量回复保留 `5.6 / 2.4 / 3.4 / 1.6`，长回答分为 4 段并标识 10 个公式，真正的 1/2/3 步骤仍正常。申论问题追踪与统一 Word/PDF 导出保持完成。Node 24 全量 665/665、申论专项 Python 15/15、前端专项 28/28；当前只等待用户最终视觉确认，不得提前完成主 Goal。
  - 首页结构保持不动。按用户最新指令，已停止“四张能力图 + 中心西西”改版，并从任务补丁记录恢复此前版本：原三栏卖点、学习证据旋转圆环、五章滚动切换和中间粒子聚散全部回归；中间直接使用用户所指第二张五联水彩图 `gontu-module-ink-atlas-v1.png`。旧 Three/Spark 图像粒子脚本已重新启用，浏览器实测状态为 `ready`。针对最新浏览器标注，五章滚动高度从 340vh 缩到 285vh，桌面每章约 333px；设色底图在 0.28 秒内直接切到下一张，粒子仅延迟 35ms 后点缀约 0.52 秒，不再长时间等待粒子演完才看见底图。停稳后粒子画布退场且渲染循环实测 `running=false`；像素比为 1、采样步长为 9。结尾 CTA 继续使用项目图 `gontu-cta-study-journey-v1.png`，但改为 18 秒往返的缓慢推镜、13 秒雾光漂移与轻微明暗呼吸；文案压缩为“向前，自有答案 / 每一道认真做过的题，都在照亮下一步”，标题降为半透明灵秀宋体感，按钮改为低饱和玻璃金。图片底部深雾紫灰直接接同色页脚，无硬边，并保留 `prefers-reduced-motion`。
  - “完整上下文 / 题内追问 / 学习证据”三张证明卡已拆出独立视口触发器，只在卡组真正进入画面时按 0.24 秒间隔从左到右依次出现；离开视口会重置，下次向下滑入可再次播放，减少动态效果模式下直接显示。
  - 产品差异区底部 padding 已从最高 190px 收到最高 70px，尾句后不再留大块空白；“空间几何实验室”按真实 `data/spatial-learning-modules.json` 重做为四段自动轮播：基础截面、三视图训练、自由切面、组合体切割。每 4.8 秒切换一幕，移入或聚焦暂停，四个标签可手动切换并分别呈现截面生成、投影反推、连续切面与组合体切割动画，统一进入 `/spatial-learning.html`。产品差异主标题已由倾斜 Smiley Sans 改为 `ZCOOL XiaoWei` 为首选的清峻宋体，去掉倾斜旋转并重新调整字距行距。浏览器验证四个手动切换均与对应画面一致，自动轮播由“组合体切割”回到“基础截面”；390px 下 viewport/scrollWidth 均为 390，特色条与舞台宽度分别为 358/356。五章区增加滚轮手势锁与位置回弹保护，实测单次 1200px 大滚动仍只从 01→02，第二次才到 03。
  - 用户指出三视图配图不可信后，第二幕已改为同一组四方块数据驱动的中央模型与正/侧/俯三张投影：正视图与侧视图均为“2 高 + 1 高”，俯视图为 L 形三格，三者能反推中央结构，不再使用任意几何装饰。CTA 标题改为一行“向前，自有答案”，以 `Noto Serif SC` 500 收紧字距和标点；页脚取消独立蓝灰色块，向上覆盖 118px 融入山景底部渐暗，父容器按负 margin 后的真实高度收口。桌面截图无硬断层；390px 下标题保持一行、scrollWidth=390。
  - 学习证据圆环新增四套随选择联动的主题：完整题目=朱砂、分步方法=黛青、问题追踪=赭金、题内 AI=绛紫；中央圆底色、内外描边、轨道渐变、运行光点、当前节点、眉题与收束文案同步换色。中央标题改用 `ZCOOL XiaoWei` 并以轻微同色描边增强笔锋，浏览器逐项读取四个主题的 title color 与 core background 均不同。
  - 管理员端开始视觉收口，未改动认证、用户列表、数据详情与删除接口：紫色渐变通用后台改为深墨/宣纸/黛青/朱砂/赭金的公途管理工作台；登录页使用公途印章与克制深墨背景，用户总览增加注册用户、自定义词条、申论批改三项真实聚合指标，表格、弹窗与危险操作统一视觉层级。桌面 1280px 与手机 390px 登录页实测，手机 login box 354px、scrollWidth=390；两份页面脚本解析与 doctor 均通过。

权威 Goal：`doc/goals/unified-v3-experience-repair.md`；机器状态：`.xds/goals/unified-v3-experience-repair.json`；最近验收：`doc/acceptance/V3R-6-functional-labels-and-visual-review.md`。

## 阶段状态

- Phase 1：PR #15 本地门禁证据齐全；GitHub 状态为 open、未合并，尚无外部审阅结论。
- Phase 2：PR #16 本地门禁证据齐全；GitHub 状态为 open、未合并，尚无外部审阅结论。
- Phase 3：Draft PR #17；801成语、231逻辑填空、600片段阅读、600数量已接入，本地门禁通过，待审阅。
- Phase 4：Draft PR #18；平面/空间图推和申论整改已通过本地门禁，待审阅。
- Phase 5：Draft PR #19；真实上下文、版本 Skill、消息/run、问题卡、真实 DeepSeek 与浏览器门禁均已通过，待审阅。
- Phase 6：从 Phase 5 精确提交 `4afaf90` 建立独立工作树；GT-P6-1—6 已有直接证据，Draft PR #21 已建立，干净总装回归已完成，尚待外部审阅。

## Phase 6 当前基线

- Phase 5 最终复核：Python 61/61、Node 620/620、Goal lint 通过，分支与远端均为 `4afaf90`。
- 默认 shell 仍指向 Node 18 / Python 3.9；使用项目要求的 Node 24 / Python 3.12 后 doctor 为 0 fail，后续需把运行入口与版本提示收口。
- GT-P6-1A 已完成：桌面跨平台配置、Python 解析与 Windows 启动入口相关门禁全部通过；跨平台脚本由 8/16 提升为 14/16。
- GT-P6-1B 已完成：提交 Node/Python 版本入口并补 Windows 忽略项；跨平台脚本提升为 15/16。
- GT-P6-4A 已完成：公开固定 JWT 密钥已删除；旧密钥伪造失败、本地0600随机密钥跨重启稳定、生产缺密钥/旧密钥 fail closed 均有直接测试。
- GT-P6-1C 已完成：AI教练57项Ruff错误归零，AI/空间mypy归零，专项19/19，跨平台16/16。
- GT-P6-1D 已完成：Ruff、mypy 26文件、Bandit中高危、依赖、Python64/64、Node620/620、跨平台16/16、Goal lint全部通过。
- GT-P6-1E 已完成：无缓存静态服务支持独立端口/open/serve，正式数量与AI页HTTP200；Python venv助手可移植且命令不再悬空。
- GT-P6-1F 已完成：批准导出正确识别600题全量视觉核验，portable导出会归一化审计字段；数量CI在没有未跟踪output时直接执行60套/600题/71媒体/答案与42题解析视觉边界门禁，并能拒绝当前1800个陈旧字段。
- GT-P6-1G 已完成：600题portable seed只迁移 `answer_audit_tier`、`answer_requires_original_recheck`、`processing_stage` 三个审计字段；答案、题文与71媒体零变化。干净工作树数量CI通过，42题解析视觉边界仍为 `incomplete`。
- GT-P6-2A 已完成：平面图推、申论和AI教练统一API基址合同；独立8897端口与临时DB回归中，平面题写入1、AI线程/消息各写入1，申论无Key按预期未写正式批改记录，未再串到8888；23项页面/认证测试通过。
- PR #19 描述已校正为Phase 5真实DeepSeek、Python61/61、Node620/620和Phase 6已启动的当前事实。
- GT-P6-5A 已完成：数量与片段阅读所有题库读取统一映射为503 `bank_unavailable`；缺失、损坏、计数异常、路径/原异常脱敏、401认证优先、深层路由和健康404均有专项证据。新增3项负路径、数量4项、片段阅读8项均通过。
- GT-P6-5B 已完成：申论新增认证catalog事实接口；缺失/损坏题源在列表、详情、批改、带题聊天统一503 `question_source_unavailable`，未知题统一404 `question_not_provided`，响应不泄露路径/异常且AI不被调用；10题summary-only与答案脱敏保持。申论7项通过。
- GT-P6-5C 已完成：申论侧栏持续显示catalog题数与材料摘要说明；题源不可用会清空题目/选中态并显示明确状态，未知题显示“未提供”，批改动作被禁用且不造占位题；UI/认证11项通过。
- GT-P6-5D 已完成：申论评分严格校验5个维度、合法评级、非空总评和恰好3条建议；Provider超时、无效输出与不可用统一返回脱敏安全码，第三次无效输出不再伪装为成功；评分/聊天/分析失败不写学习事实，薄弱点分析失败返回 `unavailable` 且建议为空。专项10项、Python分进程72项、跨平台16/16、Node页面/认证11项通过。
- GT-P6-5E 已完成：申论正式批改要求合法 `Idempotency-Key`，并按 `users.id + key` 保存请求哈希和 pending/completed/failed 状态；同键同载荷回放原结果且不再调用Provider，同键不同载荷409，处理中409，失败安全回放；成功结果、历史、问题本、跨模块活动/证据/任务与幂等完成态同事务提交，保存失败全部回滚。专项14项、Python分进程76项通过。
- GT-P6-5F 已完成：正式申论界面每次有意批改生成一次UUID请求键，网络异常与 `idempotency_in_progress` 自动以同键重试；未决作答与键保持绑定，失败恢复输入，安全错误对象显示人类可读文案。Node页面/认证13项通过；8898独立端口临时DB浏览器实测仅写1条36位failed请求，界面无 `[object Object]`，历史/问题/活动/证据均为0。
- GT-P6-2B 已完成：首页认证基址与正式外壳一致，静态 8089 预览保留 8888 后端例外，其他运行环境使用当前 `location.origin`。Node 页面/认证回归 14/14；全新 Chrome 用户目录在 8899 临时服务/临时数据库完成首页注册、进入申论、刷新保留登录态，数据库仅有该测试用户且无申论批改请求；服务和浏览器均已关闭。
- GT-P6-6A 已完成：正式外壳在 390px 时以“学习导航”按钮收起/展开模块入口，`Escape` 收起后焦点回到触发按钮；加入跳转至学习内容链接、统一可见焦点样式，以及全局 `prefers-reduced-motion` 降低动效规则。Node 静态/认证 9/9 通过；浏览器实测 390px 无横向溢出、展开/键盘收起正确，1024px 保留桌面导航。
- GT-P6-2—6 已完成：在独立临时 SQLite 与 8910 服务中，以真实浏览器注册 B 账号，进入言语、数量、平面图推、立体三视图、申论、AI 教练六条主路径；言语作答后刷新恢复 1/20，服务重启后登录态仍有效。B 登出后新建 C 账号，同一言语套题为 0/20、AI 对话为空；重新登录 B 后恢复 1/20。AI 未配置时消息保留并显示可重试的诚实失败态。服务端 10 个独立进程测试文件共 62/62、页面/认证静态测试 45/45 通过；六页在 390px 与 1280px 均 `scrollWidth == clientWidth`、破图数为 0，最终浏览器 console error/warn 为 0。
- GT-P6-7 已完成：已确认 Phase 1→6 的提交链连续，Phase 6 Draft PR #21 已以 Phase 5 为基线建立；PR #15—#21 都未合并（#17—#21 为 Draft），因此总装只能作为待审分支，不能声称已合并或已获得审阅。9 个 worktree 的只读盘点与三分类见 `doc/PHASE6_COMPLETION_AUDIT.md`。
- GT-P6-8 已完成（本地待审总装）：干净 `cx/phase6-clean-integration` 只包含线性 Phase 1→6 提交链与审计提交；其新建 Python 3.12 venv 后，服务端独立进程 62/62、Node 46/46、`npm run quantity:pipeline` portable 600/60/71 门禁均通过。真实浏览器在临时 8912 服务中注册 B，言语作答刷新恢复 1/20；登出后注册 C，同套题为 0/20。六个正式路由在 390px 和 1280px 均无横向溢出、破图 0、最终 console error/warn 0。临时服务、浏览器和数据库已清理。
- GT-P6-8 补救：总装首次运行发现默认 `quantity:pipeline` 仍依赖未提交的 `output/quantity-bank/raw_sets`；现已在干净树自动切换到已提交 `approved_seed` 的 portable CI，并新增回归测试，不复制或提交任何 OCR 中间产物。

## Phase 5 已完成

- 正式 `/ai-coach` 入口与公途三栏页面；不再展示假对话、假统计或静态老师回复。
- 独立提问与训练上下文两条路径：无上下文只做方法咨询；有上下文由服务端按 JWT 重建事实。
- AI 线程、消息、run、失败、重试、候选问题卡全部归属现有 `users.id`。
- Skill Registry 覆盖片段阅读、逻辑填空、数量、平面图推、空间图推、申论和综合规划。
- Run 记录 Skill ID/版本、package/bundle/context hash、provider/model/usage/结构化输出和状态。
- Provider 输出必须通过 Skill 响应 Schema；非法输出 fail closed。
- 问候、闲聊、自我评价和无证据咨询不生成弱项；真实服务端证据达到门槛后才产生候选，用户确认后才写问题本。
- 普通保存只形成 `coach_note`，客户端不能任意创建弱项。
- 综合规划直接读取本人垂直训练表，不采信客户端可写的通用活动摘要。

## 验证证据

- Python AI Coach/Skill/配置专项：20/20；全模块分进程回归：61/61。
- Node/几何全量：620/620。
- 浏览器：注册、登录、模块加载、无上下文数量新对话、缺配置失败、消息保留、重试、刷新恢复；A→退出→B 隔离→退出→A 恢复通过。
- 响应式：1440px 与 390px 均 `scrollWidth == clientWidth`。
- Goal lint：pass。

## 未完成与事实边界

- 真实 DeepSeek 已通过；本机 `.env` 权限0600且被Git忽略。用户暂未轮换聊天中曾暴露的旧 Key，后续平台允许时仍建议更换。
- 申论仍只有10道摘要题且缺套/缺题；AI 教练不会掩盖题库不完整。
- 数量仍有42题解析视觉引用待独立审计。
- `python -m unittest discover` 会因历史测试文件共享已缓存 `database.DB_PATH` 而互相污染；本次使用每文件独立进程验证，全部57项通过。
- 历史 `tests/test_cross_platform.py` 仍有8项桌面打包治理失败，不属于 Phase 5 功能改动，必须在 Phase 6 完成审计中保留。

## 下一步

1. 先确认 `rescue/recovered-final-20260718` 已推送到 GitHub，并保留 Draft PR 作为救援差异入口。
2. 从救援分支新开首页接线分支：将确认的水墨首页接入 `/`，CTA 接到 `/app`，验证首页与功能页双向往返。
3. 首页链路稳定后，再从救援分支新开 `agent/admin-vip-finalization`，选择性迁移另一工作树的管理员/VIP改动，禁止整批覆盖。
